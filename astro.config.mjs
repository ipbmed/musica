// @ts-check
import { defineConfig } from 'astro/config';

// Ajuste `site` para o seu usuário/org no GitHub Pages.
// Ex.: https://seu-usuario.github.io
export default defineConfig({
	site: 'https://example.github.io',
	base: '/ipbsong',
});
